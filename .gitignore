# ---------------------------------------------------------
# 🔒 환경 변수 및 보안 관련 (GitHub 업로드 금지)
# ---------------------------------------------------------
.env
.env.* # .env.local, .env.test 등 파생 파일도 모두 무시
!.env.example # (옵션) .env.example 파일은 업로드 허용 (변수 명세 공유용)

# ---------------------------------------------------------
# 🐍 Python 필수 무시 항목 (함께 넣어두면 좋습니다)
# ---------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pt